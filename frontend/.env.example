# Base URL of the running backend API (see backend/.env.example),
# including its versioned prefix. Copy this file to .env and adjust if
# the backend isn't running on the default port.
VITE_API_BASE_URL=http://localhost:8000/api/v1
