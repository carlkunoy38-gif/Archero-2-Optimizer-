import { PlaceholderPage } from '../components/PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      reason="Scoring-weight configuration would need a backend endpoint to read/write app.optimizer.weights — none exists yet, so there's nothing real for this page to control."
    />
  )
}
