import { PlaceholderPage } from '../components/PlaceholderPage'

export function UpgradeAdvisorPage() {
  return (
    <PlaceholderPage
      title="Upgrade Advisor"
      reason="Ranking 'what to upgrade next' needs an Upgrade Advisor on the optimizer backend, which doesn't exist yet — only the Skill Advisor does so far."
    />
  )
}
